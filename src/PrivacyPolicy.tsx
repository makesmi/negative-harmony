import React from 'react'

const PrivacyPolicy:React.FC = () => (
  <p>
    Negative Harmony application does not process or collect any personal data. 
    Only Facebook user ID is used so that users can log in into Negative Harmony application.
    Songs will be saved into database with that id.
    Other users cannot see songs or Facebook user ID.
    Connection is protected with TLS. 
  </p>
)

export default PrivacyPolicy